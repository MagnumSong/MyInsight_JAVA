package cn.studyjava.day23;

/*
* 字节输出流
* Java.io.OutputStream 所有字节输出流的超类(父类)
* 作用：从Java程序，写出文件
* 字节：这样流每次只能操作文件中的1个字节
* 写任意文件
*
* 方法都是写入的方法
* write(int b) 写入一个字节
* write(byte[] b) 写入字节数组
* write(byte[] b, int, int) 写入字节数组，开始写入的索引，int 写几个
* close()方法，关闭流对象，释放与此流相关资源
*
* 流对象，操作文件的时候，自己不做，依赖操作系统
* */
public class OutputStreamDemo {






}
